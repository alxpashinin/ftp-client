import 'package:flutter/cupertino.dart';

class CPNewScreen extends StatelessWidget {
  const CPNewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => CupertinoPageScaffold(
        child: Column(
          children: [],
        ),
      );
}
