import 'package:flutter/material.dart';

class MTNewScreen extends StatelessWidget {
  const MTNewScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
